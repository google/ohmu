
pi = 3;   // The biblical pi

circle = \@self struct {
  radius = 10;

  area(): Int -> 2 * pi * radius;
};

circleArea(): Int -> circle.area();

getArea(c: circle): Int -> c.area();

foo(a: Int, b: Int): Int -> a + b;

bar(a: Int, b: Int): Int -> foo(a, b) * foo(b, a);